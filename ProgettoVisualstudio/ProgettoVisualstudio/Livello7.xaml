<UserControl x:Class="ProgettoVisualstudio.Livello7"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgettoVisualstudio"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label x:Name="indovinello"
               Grid.Row="1"
               Content="Cosa aumenta più viene tolto?"
               HorizontalAlignment="Center"
               FontSize="24"
               FontWeight="Bold"
               Margin="0,0,0,20"/>

        <TextBox x:Name="txtRisposta"
                 Grid.Row="2"
                 Width="350"
                 Height="40"
                 FontSize="16"
                 Padding="10"
                 HorizontalAlignment="Center"
                 Background="White"
                 BorderBrush="#888"
                 BorderThickness="1"/>

        <Button x:Name="btnConferma"
                Grid.Row="3"
                Content="Conferma"
                Width="150"
                Height="40"
                Margin="0,20,0,0"
                FontSize="16"
                Background="#4CAF50"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"
                HorizontalAlignment="Center" Click="btnConferma_Click"/>
    </Grid>
</UserControl>


